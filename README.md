### **TMDB Revenue Prediction App**

**Project Overview**

This application predicts the box office revenue of movies based on metadata such as budget, runtime and vote average. It uses a machine learning model trained on TMDB data and provides an interactive interface via Streamlit.

**Team Members**

Maïssa Benameur

To ensure a clean, isolated, and fully reproducible development setup, the project was developed using a Python virtual environment (venv). It prevents dependency conflicts with system‑wide Python packages and guarantees that the application runs with the exact versions required.



